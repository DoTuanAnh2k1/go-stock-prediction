# Copy real secrets here; apply with: helm upgrade api-svc deploy/helm/api-svc ... -f values-secret.yaml (git-ignored)
# Generate strong values:
#   jwtSecret:      openssl rand -base64 48
#   internalSecret: openssl rand -base64 48
secrets:
  jwtSecret: REPLACE_ME
  internalSecret: REPLACE_ME
