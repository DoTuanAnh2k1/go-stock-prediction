apiVersion: v2
name: service-mgt
description: Go service registry/discovery (gRPC :8121) — chart độc lập, cài riêng.
type: application
version: 0.1.0
dependencies:
  - name: common
    version: 0.1.0
    repository: "file://../common"
