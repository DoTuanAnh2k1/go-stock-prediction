# Copy real secrets here; apply with: helm upgrade cli-svc deploy/helm/cli-svc ... -f values-secret.yaml (git-ignored)
# internalSecret MUST match api-svc/auth-svc internalSecret.
secrets:
  internalSecret: REPLACE_ME
