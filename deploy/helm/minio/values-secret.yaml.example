# Copy real secrets here; apply with: helm upgrade minio deploy/helm/minio ... -f values-secret.yaml (git-ignored)
# minioRootUser / minioRootPassword MUST match prediction-svc s3AccessKey / s3SecretKey.
# Generate: openssl rand -base64 24
secrets:
  minioRootUser: REPLACE_ME
  minioRootPassword: REPLACE_ME
