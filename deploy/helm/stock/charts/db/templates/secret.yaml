# =============================================================================
# db — creds cho TimescaleDB/Postgres. envFrom secretRef trong statefulset.yaml.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.global.secrets.postgresUser | quote }}
  POSTGRES_PASSWORD: {{ .Values.global.secrets.postgresPassword | quote }}
  POSTGRES_DB: {{ .Values.global.secrets.postgresDb | quote }}
