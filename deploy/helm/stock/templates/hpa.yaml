{{- if .Values.global.hpa.enabled }}
# =============================================================================
# CKAD Lab 2.3 (áp THẬT) — HorizontalPodAutoscaler cho service request-driven:
# api-svc (HTTP dashboard + SSE) và gateway-svc (proxy). autoscaling/v2, CPU %.
#
# ⚠️ Service project chủ yếu I/O-bound / idle (1-4m CPU vs requests 50-100m) → CPU-HPA
#    CHỈ scale khi có traffic spike THẬT; lúc thường nằm ở minReplicas (giữ HA). Muốn thấy
#    scale-up phải sinh tải thật (xem day_2 loadgen). Default TẮT (global.hpa.enabled=false).
#
# HPA sở hữu .spec.replicas ⇒ deployment ĐÃ omit `replicas` khi hpa.enabled (tránh helm
# upgrade giành lại → flapping). api-svc blue/green: HPA mỗi màu (màu idle nằm ở min).
# =============================================================================
{{- $a := .Values.global.hpa.apiSvc }}
{{- $apiTargets := ternary (list "api-svc-blue" "api-svc-green") (list "api-svc") .Values.global.bluegreen.enabled }}
{{- range $name := $apiTargets }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
  labels: { app: api-svc }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: {{ $name }} }
  minReplicas: {{ $a.minReplicas }}
  maxReplicas: {{ $a.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ $a.targetCPU }} }
---
{{- end }}
{{- $g := .Values.global.hpa.gatewaySvc }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gateway-svc
  namespace: {{ .Release.Namespace }}
  labels: { app: gateway-svc }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: gateway-svc }
  minReplicas: {{ $g.minReplicas }}
  maxReplicas: {{ $g.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ $g.targetCPU }} }
{{- end }}
