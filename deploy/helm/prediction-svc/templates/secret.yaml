# =============================================================================
# prediction-svc — creds DB + MinIO S3.
# (plaintext stringData — education only; production dùng Vault/SealedSecret)
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: prediction-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: {{ required "prediction-svc: secrets.postgresPassword is required (provide via -f values-secret.yaml)" .Values.secrets.postgresPassword | quote }}
  # MinIO credentials — PHẢI khớp minio-secret (MINIO_ROOT_USER/PASSWORD).
  # S3_SECRET_KEY trước để "minioadmin" (sai) -> 403 Forbidden khi up/download model.
  S3_ACCESS_KEY: {{ required "prediction-svc: secrets.s3AccessKey is required (provide via -f values-secret.yaml)" .Values.secrets.s3AccessKey | quote }}
  S3_SECRET_KEY: {{ required "prediction-svc: secrets.s3SecretKey is required (provide via -f values-secret.yaml)" .Values.secrets.s3SecretKey | quote }}
