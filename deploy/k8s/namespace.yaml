# =============================================================================
# Namespace stock — chứa toàn bộ resource của hệ thống trong cluster kind.
# Apply file này ĐẦU TIÊN (mọi resource khác đều namespace: stock).
#   kubectl apply -f deploy/k8s/namespace.yaml
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: stock
  labels:
    app.kubernetes.io/part-of: go-stock-prediction
