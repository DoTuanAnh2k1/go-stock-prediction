# =============================================================================
# prediction-svc — ClusterIP gRPC :8119 (api-svc gọi qua GRPC_TARGET).
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: prediction-svc
  namespace: stock
spec:
  selector: { app: prediction-svc }
  ports:
    - port: 8119
      targetPort: 8119
