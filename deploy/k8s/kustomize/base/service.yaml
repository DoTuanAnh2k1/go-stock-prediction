# ClusterIP cho api-kz (N1). Selector khớp Pod label → Endpoints không mồ côi (N5).
apiVersion: v1
kind: Service
metadata:
  name: api-kz
spec:
  type: ClusterIP
  selector:
    app: api-kz
  ports:
    - name: http
      port: 8118
      targetPort: 8118
