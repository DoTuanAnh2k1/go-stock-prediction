# =============================================================================
# api-svc — ClusterIP Service, expose :8118 trong cluster.
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: api-svc
  namespace: stock
spec:
  selector: { app: api-svc }
  ports:
    - port: 8118
      targetPort: 8118
