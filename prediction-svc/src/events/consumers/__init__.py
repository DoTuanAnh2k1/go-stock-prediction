"""Kafka consumers — each runs as its own process/Deployment.

Entrypoints:
  python -m src.events.consumers.predict
  python -m src.events.consumers.reconcile
  python -m src.events.consumers.simulation
"""
